version: "3.8"

services:
  postgres:
    image: postgres:16-alpine
    container_name: flyaway-travel-db
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: flyaway-db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
